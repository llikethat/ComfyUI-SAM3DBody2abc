# MegaSAM Integration Workflow Diagram

## Overview

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                         SAM3DBody2abc + MegaSAM Pipeline                        │
└─────────────────────────────────────────────────────────────────────────────────┘

                                    INPUT VIDEO
                                         │
                                         ▼
                    ┌─────────────────────────────────────────┐
                    │         Load Video Frames               │
                    │      IMAGE [N, H, W, 3] float32         │
                    │      RAM: N * H * W * 3 * 4 bytes       │
                    └─────────────────────────────────────────┘
                                         │
                    ┌────────────────────┼────────────────────┐
                    │                    │                    │
                    ▼                    ▼                    ▼
    ┌───────────────────────┐  ┌─────────────────┐  ┌───────────────────────┐
    │   SAM3DBody Process   │  │  SAM3 Segment   │  │  🎬 MegaSAM Solver   │
    │   (Body Estimation)   │  │  (Person Mask)  │  │  (Camera + Depth)    │
    └───────────────────────┘  └─────────────────┘  └───────────────────────┘
                    │                    │                    │
                    ▼                    │                    │
    ┌───────────────────────┐           │                    │
    │   MESH_SEQUENCE       │           │                    │
    │   (vertices, joints)  │◄──────────┘                    │
    └───────────────────────┘                                │
                    │                                        │
                    ▼                                        ▼
    ┌───────────────────────┐            ┌───────────────────────────────────┐
    │  Motion Analyzer      │◄───────────│  CAMERA_EXTRINSICS (6-DOF)       │
    │  (trajectory, speed)  │            │  + depth_maps [N,H,W,1]          │
    └───────────────────────┘            │  + motion_masks [N,H,W,1]        │
                    │                    └───────────────────────────────────┘
                    ▼
    ┌───────────────────────┐
    │   Export Animated FBX │
    │   (Maya/Blender)      │
    └───────────────────────┘
```

---

## MegaSAM Solver Internal Pipeline

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                        🎬 MegaSAM Camera Solver (Detailed)                      │
└─────────────────────────────────────────────────────────────────────────────────┘

INPUT: images [N, H, W, 3] float32 (0-1 range)
       ├── N = number of frames (e.g., 100-500)
       ├── H = height (e.g., 720, 1080)
       └── W = width (e.g., 1280, 1920)

═══════════════════════════════════════════════════════════════════════════════════
STAGE 1: MONO-DEPTH ESTIMATION (Depth Anything V2)
═══════════════════════════════════════════════════════════════════════════════════

    ┌─────────────────────────────────────────────────────────────────┐
    │  Depth Anything ViT-L                                           │
    │  Model Size: ~350MB VRAM                                        │
    │  Per-frame: ~2GB VRAM at 1080p                                  │
    └─────────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    │     CHUNKED PROCESSING        │
                    │  chunk_size: 4-16 frames      │
                    │  (configurable by user)       │
                    └───────────────────────────────┘
                                    │
    For each chunk [i : i+chunk_size]:
        ├── Load frames to GPU [chunk_size, 3, H, W]
        ├── Run Depth Anything inference
        ├── Store disparity maps to CPU [chunk_size, H, W]
        ├── Clear GPU cache
        └── Progress: i / N frames

                                    │
                                    ▼
    OUTPUT: mono_disparity [N, H, W] float32
            ├── Stored on CPU (RAM)
            ├── RAM: N * H * W * 4 bytes
            └── Example: 100 frames @ 1080p = ~830 MB RAM

═══════════════════════════════════════════════════════════════════════════════════
STAGE 2: METRIC DEPTH ALIGNMENT (UniDepth) - OPTIONAL
═══════════════════════════════════════════════════════════════════════════════════

    ┌─────────────────────────────────────────────────────────────────┐
    │  UniDepth (Metric Depth + Intrinsics)                           │
    │  Model Size: ~500MB VRAM                                        │
    │  Per-frame: ~3GB VRAM at 1080p                                  │
    │  NOTE: Can skip if focal_length provided                        │
    └─────────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    │     SPARSE SAMPLING           │
                    │  Process every Nth frame      │
                    │  (N = 10-20 for efficiency)   │
                    └───────────────────────────────┘
                                    │
    OUTPUT: scale, shift (per-frame alignment)
            intrinsic_matrix [3, 3]
            estimated_fov (degrees)

═══════════════════════════════════════════════════════════════════════════════════
STAGE 3: CAMERA TRACKING (DROID-SLAM)
═══════════════════════════════════════════════════════════════════════════════════

    ┌─────────────────────────────────────────────────────────────────┐
    │  DROID-SLAM (Modified for depth guidance)                       │
    │  Model Size: ~150MB VRAM                                        │
    │  Internal buffers: ~2-4GB VRAM (correlation volumes)            │
    │  Processes SEQUENTIALLY (streaming)                             │
    └─────────────────────────────────────────────────────────────────┘
                                    │
    ┌───────────────────────────────┴───────────────────────────────┐
    │                    SEQUENTIAL PROCESSING                       │
    │                                                                │
    │  For frame t = 0, 1, 2, ..., N-1:                             │
    │      ├── Load image[t] + depth[t] to GPU                      │
    │      ├── Resize to tracking resolution (384 x 512 typical)    │
    │      ├── Track frame (update pose estimate)                   │
    │      ├── Keyframe selection (motion threshold)                │
    │      └── Bundle adjustment (periodic, every ~10 frames)       │
    │                                                                │
    │  Memory Management:                                            │
    │      ├── max_keyframes: 96-256 (configurable)                 │
    │      ├── Older keyframes removed when buffer full             │
    │      └── Final global BA uses all keyframes                   │
    └────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
    OUTPUT: poses [N, 7] (quaternion + translation in SE3)
            motion_probability [N, H, W] (dynamic region mask)
            intrinsics [N, 4] (fx, fy, cx, cy) - refined

═══════════════════════════════════════════════════════════════════════════════════
STAGE 4: OPTICAL FLOW (RAFT) - For CVD
═══════════════════════════════════════════════════════════════════════════════════

    ┌─────────────────────────────────────────────────────────────────┐
    │  RAFT Optical Flow                                              │
    │  Model Size: ~20MB VRAM                                         │
    │  Per-pair: ~1.5GB VRAM at 1080p                                │
    └─────────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    │     CHUNKED PAIR PROCESSING   │
                    │  Process frame pairs in batch │
                    │  chunk_size: 8-16 pairs       │
                    └───────────────────────────────┘
                                    │
    Frame pairs to compute:
        ├── Adjacent: (0,1), (1,2), ..., (N-2, N-1)
        ├── Skip-1: (0,2), (1,3), ..., (N-3, N-1)  [optional]
        └── Total: ~N to 2N pairs

                                    │
                                    ▼
    OUTPUT: flows [num_pairs, 2, H, W] float32
            flow_masks [num_pairs, H, W] float32
            ├── Stored on CPU initially
            └── RAM: num_pairs * 3 * H * W * 4 bytes

═══════════════════════════════════════════════════════════════════════════════════
STAGE 5: CONSISTENT VIDEO DEPTH (CVD Optimization) - OPTIONAL
═══════════════════════════════════════════════════════════════════════════════════

    ┌─────────────────────────────────────────────────────────────────┐
    │  CVD Optimization (Gradient Descent)                            │
    │  Optimizes: disparity, uncertainty, (optionally poses)          │
    │  Iterations: 100 (pose) + 400 (depth)                          │
    │  VRAM HEAVY: Needs all data on GPU simultaneously              │
    └─────────────────────────────────────────────────────────────────┘
                                    │
    ┌───────────────────────────────┴───────────────────────────────┐
    │                    MEMORY OPTIMIZATION                         │
    │                                                                │
    │  Option A: Full Resolution (HIGH VRAM)                        │
    │      ├── All frames + flows on GPU                            │
    │      ├── VRAM: ~8-16 GB for 100 frames @ 1080p               │
    │      └── Best quality                                         │
    │                                                                │
    │  Option B: Half Resolution (MEDIUM VRAM)                      │
    │      ├── Downsample 2x before optimization                    │
    │      ├── VRAM: ~2-4 GB for 100 frames                        │
    │      ├── Upsample results after                               │
    │      └── Good quality, faster                                 │
    │                                                                │
    │  Option C: Skip CVD (LOW VRAM)                                │
    │      ├── Use DROID depth directly                             │
    │      ├── VRAM: ~2 GB                                          │
    │      └── Acceptable quality for camera-only use               │
    └────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
    OUTPUT: depths_refined [N, H, W] float32
            cam_c2w [N, 4, 4] float32 (camera-to-world matrices)

═══════════════════════════════════════════════════════════════════════════════════
STAGE 6: COORDINATE CONVERSION & OUTPUT
═══════════════════════════════════════════════════════════════════════════════════

    ┌─────────────────────────────────────────────────────────────────┐
    │  Convert OpenCV → Maya/Blender coordinates                      │
    │  Extract Euler angles (pan/tilt/roll) from rotation matrices   │
    │  Build CAMERA_EXTRINSICS dict                                  │
    └─────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
    FINAL OUTPUTS:
    
    ┌─────────────────────────────────────────────────────────────────┐
    │  camera_extrinsics: CAMERA_EXTRINSICS                          │
    │      ├── num_frames: int                                       │
    │      ├── rotations: List[Dict] with pan/tilt/roll/tx/ty/tz    │
    │      ├── has_translation: True                                 │
    │      ├── source: "MegaSAM"                                     │
    │      └── focal_length_px, principal_point                      │
    └─────────────────────────────────────────────────────────────────┘
    
    ┌─────────────────────────────────────────────────────────────────┐
    │  depth_maps: IMAGE [N, H, W, 1]                                │
    │      ├── Normalized 0-1 for visualization                      │
    │      └── Raw metric depth available in metadata                │
    └─────────────────────────────────────────────────────────────────┘
    
    ┌─────────────────────────────────────────────────────────────────┐
    │  motion_masks: IMAGE [N, H, W, 1]                              │
    │      ├── 0 = static background                                 │
    │      └── 1 = dynamic foreground (person, etc.)                │
    └─────────────────────────────────────────────────────────────────┘
```

---

## Memory Requirements Summary

### VRAM Usage by Stage

| Stage | Model VRAM | Working VRAM | Peak VRAM |
|-------|------------|--------------|-----------|
| Depth Anything | 350 MB | 2 GB/frame | ~3 GB |
| UniDepth | 500 MB | 3 GB/frame | ~4 GB |
| DROID-SLAM | 150 MB | 2-4 GB buffers | ~5 GB |
| RAFT Flow | 20 MB | 1.5 GB/pair | ~2 GB |
| CVD Optimization | - | 8-16 GB total | **~12 GB** |

### RAM Usage

| Data | Size Formula | Example (100 frames @ 1080p) |
|------|--------------|------------------------------|
| Input Images | N × H × W × 3 × 4 | 2.5 GB |
| Mono Disparity | N × H × W × 4 | 830 MB |
| Optical Flows | pairs × 2 × H × W × 4 | 1.6 GB |
| Flow Masks | pairs × H × W × 4 | 830 MB |
| **Total RAM** | | **~6 GB** |

---

## Quality vs Memory Presets

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           QUALITY PRESETS                                       │
└─────────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────────┐
│  🚀 FAST (Low VRAM) - 6 GB VRAM minimum                                        │
│                                                                                 │
│  Settings:                                                                      │
│      ├── depth_chunk_size: 1                                                   │
│      ├── skip_unidepth: True (use provided focal or estimate)                  │
│      ├── tracking_resolution: 320 × 240                                        │
│      ├── flow_chunk_size: 4                                                    │
│      ├── skip_cvd: True                                                        │
│      └── max_frames: 200                                                       │
│                                                                                 │
│  Use for: Quick preview, testing, low-end GPUs                                 │
│  Quality: ★★☆☆☆                                                               │
└─────────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────────┐
│  ⚖️ BALANCED (Medium VRAM) - 8-10 GB VRAM recommended                          │
│                                                                                 │
│  Settings:                                                                      │
│      ├── depth_chunk_size: 4                                                   │
│      ├── skip_unidepth: False (sample every 10th frame)                        │
│      ├── tracking_resolution: 384 × 512                                        │
│      ├── flow_chunk_size: 8                                                    │
│      ├── cvd_resolution: 0.5 (half res)                                        │
│      └── max_frames: 500                                                       │
│                                                                                 │
│  Use for: Production use, most workflows                                       │
│  Quality: ★★★★☆                                                               │
└─────────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────────┐
│  🎯 BEST (High VRAM) - 12+ GB VRAM required                                    │
│                                                                                 │
│  Settings:                                                                      │
│      ├── depth_chunk_size: 8                                                   │
│      ├── skip_unidepth: False (every frame)                                    │
│      ├── tracking_resolution: 480 × 640                                        │
│      ├── flow_chunk_size: 16                                                   │
│      ├── cvd_resolution: 1.0 (full res)                                        │
│      └── max_frames: 1000                                                      │
│                                                                                 │
│  Use for: Final output, complex shots, high accuracy needed                    │
│  Quality: ★★★★★                                                               │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## Node Interface Design

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│  🎬 MegaSAM Camera Solver                                                       │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│  REQUIRED INPUTS                                                                │
│  ══════════════                                                                 │
│  images ─────────────────────────────────○  IMAGE [N, H, W, 3]                 │
│                                                                                 │
│  OPTIONAL INPUTS                                                                │
│  ═══════════════                                                                │
│  foreground_masks ───────────────────────○  MASK [N, H, W] (exclude from flow) │
│                                                                                 │
│  PARAMETERS                                                                     │
│  ══════════                                                                     │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │ coordinate_system    [Maya (Y-up) ▼]                                    │   │
│  │ quality_preset       [Balanced ▼]     ← Fast / Balanced / Best          │   │
│  │ focal_length_px      [0.0        ]    ← 0 = auto-estimate               │   │
│  │ optimize_focal       [✓]              ← refine during tracking          │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
│                                                                                 │
│  ADVANCED (collapsed by default)                                                │
│  ════════════════════════════════                                               │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │ depth_chunk_size     [4          ]    ← frames per batch for depth      │   │
│  │ flow_chunk_size      [8          ]    ← pairs per batch for flow        │   │
│  │ max_keyframes        [128        ]    ← DROID buffer size               │   │
│  │ tracking_resolution  [384        ]    ← height for tracking             │   │
│  │ run_cvd              [✓]              ← consistent video depth          │   │
│  │ cvd_resolution       [0.5        ]    ← 0.5 = half, 1.0 = full          │   │
│  │ cvd_iterations       [400        ]    ← optimization steps              │   │
│  │ model_path           [           ]    ← custom checkpoint dir           │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
│                                                                                 │
│  OUTPUTS                                                                        │
│  ═══════                                                                        │
│  ○─────────────────────────── camera_extrinsics  CAMERA_EXTRINSICS             │
│  ○─────────────────────────── depth_maps         IMAGE [N, H, W, 1]            │
│  ○─────────────────────────── motion_masks       IMAGE [N, H, W, 1]            │
│  ○─────────────────────────── status             STRING                        │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## Workflow Examples

### Example 1: Simple Dolly Shot (Camera + Body)

```
┌──────────────┐     ┌─────────────────┐     ┌──────────────────┐
│ Load Video   │────▶│ MegaSAM Solver  │────▶│ Motion Analyzer  │
│ (100 frames) │     │ preset=Balanced │     │                  │
└──────────────┘     └─────────────────┘     └──────────────────┘
       │                     │                        │
       │                     │                        ▼
       ▼                     │              ┌──────────────────┐
┌──────────────┐             │              │ Export FBX       │
│ SAM3DBody    │─────────────┼─────────────▶│ (camera+body)    │
│ Process      │             │              └──────────────────┘
└──────────────┘             │
                             ▼
                    camera_extrinsics ──────▶ [to Motion Analyzer]
                    depth_maps ─────────────▶ [preview/debug]
                    motion_masks ───────────▶ [person mask]
```

### Example 2: Complex Handheld (Full Pipeline)

```
┌──────────────┐     ┌─────────────────┐
│ Load Video   │────▶│ MegaSAM Solver  │
│ (300 frames) │     │ preset=Best     │
└──────────────┘     │ run_cvd=True    │
       │             └─────────────────┘
       │                     │
       ▼                     ├──▶ camera_extrinsics ──┐
┌──────────────┐             │                        │
│ SAM3 Segment │──┐          ├──▶ depth_maps ────────┼──▶ [Quality Check]
└──────────────┘  │          │                        │
       │          │          └──▶ motion_masks ──────┼──▶ [Mask Person]
       ▼          │                                   │
┌──────────────┐  │                                   │
│ SAM3DBody    │──┼───────────────────────────────────┘
│ Process      │  │                    │
└──────────────┘  │                    ▼
       │          │           ┌──────────────────┐
       ▼          └──────────▶│ Motion Analyzer  │
┌──────────────┐              │ depth=MegaSAM    │
│ Trajectory   │─────────────▶│                  │
│ Tracker      │              └──────────────────┘
└──────────────┘                       │
                                       ▼
                              ┌──────────────────┐
                              │ Export FBX       │
                              │ full pipeline    │
                              └──────────────────┘
```

---

## Error Handling & Fallbacks

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           ERROR HANDLING FLOW                                   │
└─────────────────────────────────────────────────────────────────────────────────┘

MegaSAM Solver Start
        │
        ▼
┌───────────────────┐     NO      ┌───────────────────────────────────┐
│ Dependencies OK?  │────────────▶│ FALLBACK: Return identity cameras │
│ (lietorch,kornia) │             │ Status: "⚠ Missing dependencies" │
└───────────────────┘             └───────────────────────────────────┘
        │ YES
        ▼
┌───────────────────┐     NO      ┌───────────────────────────────────┐
│ Checkpoints exist?│────────────▶│ FALLBACK: Return identity cameras │
│                   │             │ Status: "⚠ Missing checkpoints"  │
└───────────────────┘             └───────────────────────────────────┘
        │ YES
        ▼
┌───────────────────┐     NO      ┌───────────────────────────────────┐
│ VRAM sufficient?  │────────────▶│ Auto-reduce quality preset        │
│ (check before run)│             │ Status: "⚠ Reduced to Fast mode" │
└───────────────────┘             └───────────────────────────────────┘
        │ YES
        ▼
┌───────────────────┐
│ Run Pipeline      │
│ (with chunking)   │
└───────────────────┘
        │
        ▼
┌───────────────────┐     YES     ┌───────────────────────────────────┐
│ OOM Error?        │────────────▶│ Reduce chunk size, retry          │
│                   │             │ If still fails: skip CVD stage    │
└───────────────────┘             └───────────────────────────────────┘
        │ NO
        ▼
    SUCCESS
```
